<!DOCTYPE html>
<html>
  <head>
    <title>Denyse Hadley Spiritial Direction and Coaching</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://fonts.googleapis.com/css2?family=Merriweather:ital,wght@0,300;0,400;0,700;1,300;1,400;1,700&display=swap" rel="stylesheet">
  </head>

  <body>
    <header>
      <%= link_to 'Denyse Hadley Spiritual Direction and Coaching', root_path, class: "home-link #{'white' if current_page?(root_path)}" %>
      <nav>
        <ul>
          <li class="mobile-only">
            <%= link_to 'Home', root_path, class: ('active' if current_page?(root_path)) %>
          </li>
        </ul>
      </nav>
      <%= link_to 'Contact', contact_index_path, class: 'contact-link' %>
    </header>

    <div class="content">
      <%= yield %>
    </div>

    <% unless current_page?(contact_index_path) %>
      <footer>
        <h2>Let's connect.</h2>
        <%= link_to 'Schedule a Consultation', contact_index_path, class: 'button' %>
      </footer>
    <% end %>
  </body>
</html>
